<h2>Index</h2>
<table>
	<thead>
		<tr><td>ID</td><td>Status</td><td>Ship It!</tr>
	</thead>
	<tbody>
		<% @orders.each do |order| %>
			<tr><td><%=order.id%></td><td><%=order.status %></td><td><%=link_to 'Ship it!', "/orders/#{order.id}/ship", class:"btn"%></tr>
		<%end%>
	</tbody>
</table>
